/**
 * MSW Handlers — DEACTIVATED
 *
 * Backend real Supabase conectado.
 * Estos handlers se mantienen vacíos según el protocolo de AGENTS.md.
 * Para reactivar mocking, agrega interceptores aquí.
 */
export const handlers = [];
